# Local dev: Vite proxies /api → http://localhost:5000
VITE_API_URL=/api

# Production example (must include host; /api is appended automatically if missing):
# VITE_API_URL=https://savoriabackend.sengarinfotech.com
